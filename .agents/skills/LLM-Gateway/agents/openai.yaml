interface:
  display_name: "LLM Gateway"
  short_description: "Repo-specific patterns and workflows for LLM-Gateway"
  default_prompt: "Use the LLM-Gateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true